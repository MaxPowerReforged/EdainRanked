import navBarFile from './En/TheNavBar';
import rankingFile from './En/ranking';
import replaysFile from './En/replays';

export default {
    home: 'Homepage',
    test: 'Testing',
    navBar: navBarFile,
    ranking: rankingFile,
    replays: replaysFile
}